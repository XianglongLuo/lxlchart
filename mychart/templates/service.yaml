apiVersion: v1

kind: Service

metadata:

  name: kube-node-serviceforgo

  labels:

    name: kube-node-serviceforgo

spec:

  type: NodePort      

  ports:

  - port: 80          
    targetPort: 8080  
    protocol: TCP

    nodePort: 32143   

  selector:

    app: goweb         

